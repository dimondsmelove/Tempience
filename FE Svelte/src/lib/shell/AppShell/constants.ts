/** Flowbite consumes the same editable roles as Tempience controls. */
export const shellTheme = {
	button: {
		base: 'cg-control font-medium focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent'
	},
	badge: {
		base: 'rounded-[var(--cg-radius-control)] px-1.5 text-[length:var(--cg-text-size-caption)] font-medium'
	}
};

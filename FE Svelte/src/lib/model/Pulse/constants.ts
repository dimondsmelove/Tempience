/** The pulse runs this long, once: the ring on the ribbon, the flash of the name in the rail. */
export const PULSE_MS = 600;
/** The ring grows from the selection ring's gap outward by this much while it fades. */
export const PULSE_EXPAND_PX = 6;

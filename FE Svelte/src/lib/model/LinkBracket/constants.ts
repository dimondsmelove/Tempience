/** The channel of a bracket runs this far from the selected row's centre (research п. 15). */
export const LINK_CHANNEL_PX = 22;
/** A bracket leaves and meets a mark this far outside its silhouette, as the selection ring does. */
export const LINK_GAP_PX = 2;
/** The bar that ends a bracket at its target. */
export const LINK_TICK_WIDTH_PX = 5;
export const LINK_TICK_HEIGHT_PX = 1.5;

/** Prefix of every User Timing mark and measure the read path emits. */
export const LOAD_TIMING_PREFIX = 'tempience:load';

/** Console tag of the one summary line printed when the snapshot is ready. */
export const LOAD_TIMING_LOG_TAG = '[tempience:load]';

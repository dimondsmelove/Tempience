/** Sizes in px, the library's defaults: the core, a petal, the colour ring, the arc and its offset. */
export const CORE_SIZE = 32;
export const PETAL_SIZE = 32;
export const BAR_WIDTH = 12;
export const ARC_WIDTH = 12;
export const ARC_OFFSET = 30;
/** Two constants of the library's layout that are not options: the ring's gap and the arc's gradient stops. */
export const BAR_GAP = 20;
export const ARC_STOPS = 11;
/** The bloom, the library's default; 0 under reduced motion. */
export const BLOOM_MS = 300;
/** How the library marks a petal button, and the number its label ends with — the petal's HSL hue. */
export const PETAL_SELECTOR = '.bcp-petal';
export const PETAL_LABEL_HUE = /(\d+)$/;
/** The swatch that opens the flower: a round dot of the colour picked, 24 px. */
export const SWATCH_SIZE_PX = 24;

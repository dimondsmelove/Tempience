/** The attribute every lens source carries: the `hoverKey` of what it lights; the completeness spec reads it. */
export const LENS_ATTRIBUTE = 'data-lens';
/** A touch tap focuses a button on some phones: the focus that follows within this long lights nothing. */
export const TOUCH_FOCUS_GRACE_MS = 300;

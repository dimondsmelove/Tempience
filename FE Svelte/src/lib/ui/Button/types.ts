/** Button roles from DESIGN.md: one primary per context, default raised, quiet for secondary actions. */
export type ButtonVariant = 'primary' | 'default' | 'quiet';

/** `md` is the 32 px control from the design tokens, `sm` the 28 px toolbar control. */
export type ButtonSize = 'md' | 'sm';

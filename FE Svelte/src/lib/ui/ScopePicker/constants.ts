/** Indent per tree level in the picker's list, in pixels. */
export const PICKER_INDENT_PX = 12;
/** The list stops growing here and scrolls. */
export const PICKER_LIST_MAX_HEIGHT = '40vh';
/** The list is as wide as its control, but never narrower than this. */
export const PICKER_MIN_WIDTH_REM = 14;

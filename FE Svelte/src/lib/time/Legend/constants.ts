import type { LegendKey } from '$lib/model/Projection/types';

/** Legend items without a source in the model yet: shown, but not switchable. */
export const UNAVAILABLE_LEGEND: readonly LegendKey[] = [];

export const OVERVIEW_HEIGHT_PX = 28;
/** Density bins across the strip; one bin is about this wide. */
export const BIN_WIDTH_PX = 3;
/** The strip pads the data extent by this share on each side so the window can sit at the ends. */
export const EXTENT_PAD_RATIO = 0.04;
/** Pixels from a frame edge that still grab the edge instead of the body. */
export const EDGE_GRIP_PX = 5;
export const MIN_FRAME_PX = 6;
export const DENSITY_MAX_ALPHA = 0.85;
export const FRAME_FILL_ALPHA = 0.14;
